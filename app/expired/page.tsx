import { ExpiredView } from "@/components/expired-view";

/**
 * Renders the page view displayed when a session or item has expired.
 *
 * @returns {JSX.Element} the rendered ExpiredView component
 */
export default function ExpiredPage() {
  return <ExpiredView />;
}
