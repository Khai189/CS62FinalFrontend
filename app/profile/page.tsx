import { ProfileView } from "@/components/profile-view";

/**
 * Renders the profile page for a user's details.
 * @returns {JSX.Element} the rendered ProfileView component
 */
export default function ProfilePage() {
  return <ProfileView />;
}
