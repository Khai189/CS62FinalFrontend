import { ActivityView } from "@/components/activity-view";

/**
 * Renders the primary activity dashboard page.
 * 
 * @returns {JSX.Element} The rendered ActivityView component.
 */
export default function ActivityPage() {
  return <ActivityView />;
}
