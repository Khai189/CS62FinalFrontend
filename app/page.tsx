import { Dashboard } from "@/components/dashboard";

/**
 * The primary entry point for the application's home route.
 * 
 * @returns {JSX.Element} the rendered Home page
 */
export default function Home() {
  return <Dashboard />;
}
